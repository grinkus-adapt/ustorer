import './Logo.css';

const Logo = ({ className }) => (
  <img
    src="../../../src/assets/images/Adapt_primary_WHITE.png"
    alt="Adapt"
    className={`Logo ${className}`}
  />
);

export default Logo;
